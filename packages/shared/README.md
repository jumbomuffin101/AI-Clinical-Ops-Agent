# Shared Package

Placeholder for shared contracts between the FastAPI API and Next.js frontend. The initial implementation keeps runtime schemas in the API and mirrors TypeScript types in the dashboard.
